/**
 *
 * indexOf() -> 해당 문자열을 포함하는지 체크하는 함수로 일반적으로 많이 사용됨
 * 같은 것이 없을 경우 -1 을 리턴 존재할 경우 index값을 리턴한다.
 * @param haystack
 * @param needle
 */
var strStr = function(haystack, needle) {
    console.log(haystack.indexOf(needle));
};

const haystakc = "";
const needle = "";

strStr(haystakc, needle);
