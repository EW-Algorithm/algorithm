/**
 * Sqrt () -> 제곱근 구하는 Math 함수 ex) Math.sqrt(1024) -> 32
 * pow () -> 거듭제곱 값을 구하는 Math 함수 Math.pow(2,3) -> 2의 3승
 *
 * ceil () -> 올림
 * floor () -> 소수점 버림
 * round () -> 반올림
 * @param x
 */
const sqrtFunc = function (x) {

    console.log(Math.floor(Math.sqrt(x)));
};

sqrtFunc(4);
sqrtFunc(8);
